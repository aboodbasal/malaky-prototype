# Postgres connection string from Neon (https://neon.tech)
DATABASE_URL=

# Auth.js — generate with: openssl rand -base64 32
AUTH_SECRET=

# Resend API key for email magic links (https://resend.com)
AUTH_RESEND_KEY=

# Sender address for magic-link emails (must be a verified domain on Resend)
AUTH_EMAIL_FROM="Malaky <noreply@example.com>"

# Anthropic API key for Claude
ANTHROPIC_API_KEY=
